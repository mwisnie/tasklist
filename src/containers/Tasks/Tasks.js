import React from 'react';
import Section from './../../components/UI/Section/Section';

const tasks = props => {
  
  return (
    <Section>
      <p>Tasks container</p>
    </Section>
  );
};

export default tasks;